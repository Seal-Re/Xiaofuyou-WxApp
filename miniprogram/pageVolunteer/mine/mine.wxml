<view class="container">
  <view class="top-banner">
    <image class="banner-img" src="{{bannerImageUrl}}" mode="aspectFill" />

    <view class="avatar-box">
      <image class="avatar" src="{{avatar || '/images/mine/note.png'}}" />
      <text class="nickname" bindtap="{{!logged ? 'login' : ''}}">
        {{ displayNick || '登录/注册>' }}
      </text>
    </view>
  </view>

  <view class="welcome-block">
    <view class="top-area">
      <text class="welcome-text">
        {{ logged ? welcomeText : '欢迎~快加入小福佑志愿者吧~' }}
      </text>
    </view>

    <view class="middle-area">
      <view class="note-info">
        <text class="note-count">{{ noteCount }}</text>
        <image class="note-icon" src="/images/mine/energy.png" mode="aspectFit"></image>
      </view>
      <text class="signin-text" bindtap="goToSignin">前往志愿，获取能量></text>
    </view>

    <view class="bottom-area">
      <view class="progress-bar-container">
        <view class="progress-bar" style="width: {{progress}}"></view>
      </view>
    </view>
  </view>

  <view class="feature-list">
    <view class="feature-item" bindtap="goOrder">
      <image src="/images/mine/confirm.png" class="feature-icon" />
      <text class="feature-label">我的认证</text>
    </view>

    <view class="feature-item" bindtap="goNote">
      <image src="/images/mine/energy.png" class="feature-icon" />
      <text class="feature-label">我的能量</text>
    </view>

    <view class="feature-item" bindtap="goMyQrCode">
      <image src="/images/mine/volunteer.png" class="feature-icon" />
      <text class="feature-label">我的服务</text>
    </view>

    <view class="feature-item" bindtap="goSetting">
      <image src="/images/mine/setting.png" class="feature-icon" />
      <text class="feature-label">设置</text>
    </view>
  </view>

  <view wx:if="{{WaitingLog}}" class="waiting-toast">
    <text>登录中...</text>
  </view>

</view>